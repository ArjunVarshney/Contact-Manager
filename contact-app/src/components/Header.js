import React  from "react";

const Header = ()=>{
   return (
      <div className="ui block header sticky">
         <h1 className="ui center aligned header">Contacts</h1>
      </div>
   );
}

export default Header;